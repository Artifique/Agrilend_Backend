package com.agrilend.backend.entity.enums;

public enum TransactionType {
    TOKENIZATION,
    ESCROW_DEPOSIT,
    ESCROW_RELEASE,
    FARMER_PAYMENT,
    PLATFORM_FEE,
    STAKING_REWARD,
    REFUND,
    TOKEN_MINT,
    TOKEN_BURN,
    SCHEDULED_TRANSACTION
}